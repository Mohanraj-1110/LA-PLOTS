# MongoDB Atlas Database Connection
# Paste your MongoDB Atlas connection URI here:
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/la_plots?retryWrites=true&w=majority

# Backend Server Port (defaults to 5000 when running as standalone)
PORT=5000

# Firebase Authentication Credentials (Authentication Only)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_APP_ID=your_firebase_app_id

# Admin Email Whitelist (comma-separated)
# IMPORTANT: Only these emails will receive admin role automatically.
# If not set, NO user will be auto-assigned admin role.
VITE_ADMIN_EMAILS=admin@yourdomain.com
ADMIN_EMAILS=admin@yourdomain.com
